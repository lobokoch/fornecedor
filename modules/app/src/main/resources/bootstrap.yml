spring:
  application:
    name: cadastros-fornecedor
    
  cloud:
    config:
      uri: http://localhost:9091

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/      